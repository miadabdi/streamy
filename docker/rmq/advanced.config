%% rabbitmq consumer_timeout: streamy transcodes legitimately run longer
%% than the 30-minute default — an unacked delivery older than the timeout
%% gets its channel closed and the message redelivered (duplicate live job).
[{rabbit, [{consumer_timeout, 36000000}]}].
